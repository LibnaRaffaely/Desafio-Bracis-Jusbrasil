# avaliacao/

Métrica local (diagnóstico), os três modos (`spans`, `resolucao`, `e2e`),
geração do `erros.csv`, chamada ao `kaggle_metric.py` oficial e portão de
regressão. Detalhes em `docs/avaliacao.md`.
